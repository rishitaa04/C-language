//  to avoid repetition

/*#include <stdio.h>
int main()
{
    for (int i = 1; i <= 10; i++)
    {
        printf("Good morning \n ");
    }
    return 0;
}

har function me return 0 likhna hota hai

PROPERTIES :
    1.Execution always starts from main
    2.A function gets called directly or indirectly via main
    3.There can be multiple functions in a program


    There are two types of function types :

    1.library function - in-built

            For e.g. printf,scanf

    2.User-defined - defined by coders

            For e.g. anything


    parameter : taking value
    return : output






*/