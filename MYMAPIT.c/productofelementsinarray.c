#include <stdio.h>
int main()
{
    int n, array;
    printf("Enter the size of array : ");
    scanf("%d", &n);
    int i;
    scanf("%d", array[n]);
    for (int i = 1; i <= n; i++)
    {
    }
    return 0;
}