#include <stdio.h>
int main()
{
    int marks;

    printf("Marks scored is : ");

    scanf("%d", &marks);

    if (marks > 35 && marks < 50)

    {

        printf("Third class");
    }

    return 0;
}