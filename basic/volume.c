// Voulume of sphere

#include <stdio.h>
int main()
{

    int r;
    printf("Enter the radius of the sphere : ");
    scanf("%d", &r);
    int volume;
    float pi = 3.14;
    volume = 4 % 3 * pi * (r * 3);
    return 0;
}