// DECREASING AP

#include <stdio.h>
int main()
{
    int a = 100;
    for (int i = 1; a > 0; i++)
    {
        printf("%d\n", a);
        a = a - 3;
    }
    return 0;
}

// PROBLEMS : We don't know the number of terms to either maths lgao ya fir uper vale code ko krdo run

// using maths the code is :

/*
    #include <stdio.h>
int main()
{
    int n;
    printf("Enter any number : ");
    scanf("%d", &n);
    int a = 100;
    for (int i = 1; i < = n ; i++)
    {
        printf("%d\n", a);
        a = a - 3;
    }
    return 0;
}


*/