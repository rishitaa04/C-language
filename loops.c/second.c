#include <stdio.h>
int main()
{
    // this i is iterator or maybe counter jo count krta hai ki kaam kitni baar hua

    for (int i = 10; i >= 1; i = i - 1)
    {

        printf("%d\n", i);
    }
    return 0;
}